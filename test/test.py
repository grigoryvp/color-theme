# Comment
""" Block comment """
class Foo(object):
  _foo = [1, .1, 0xa, math.pi]
  @decorator
  def foo(bar, baz):
    for foo in [None, True]:
      Foo.prop.method(lambda v: v)
      self._foo = {
        foo: ["stri\ng", 'stri\ng']
      }
    return 10
