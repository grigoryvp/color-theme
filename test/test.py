import math

# Comment
""" Block comment """
class Foo(object):
  _foo = [1, .1, 0xa, math.pi]
  @print
  def foo(self, bar, baz):
    for foo in [None, True]:
      Foo._foo.sort(key=lambda v: v)
      self._bar = {
        foo: ["stri\ng", 'stri\ng']
      }
    return 10
